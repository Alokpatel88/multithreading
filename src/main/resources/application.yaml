spring:
  application:
    name: multithreading
#database connection
  datasource:
    url: jdbc:postgresql://localhost:5432/multithreading
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver